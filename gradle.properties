# Mengaktifkan dukungan AndroidX (Wajib untuk Jetpack Compose & Room)
android.useAndroidX=true

# Optimasi alokasi memori saat proses compile APK
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8

# Mengaktifkan paralelisme build agar proses lebih cepat
org.gradle.parallel=true
